# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DSG' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DSG
	pod 'MBProgressHUD', '~> 1.2.0'
	pod 'SDWebImage', '~> 5.0'

  target 'DSGTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DSGUITests' do
    # Pods for testing
  end

end
